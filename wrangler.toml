name = "grow-leads"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./dist"

[[triggers.crons]]
# Every 2 minutes — processes pending/scraping/building leads
crons = ["*/2 * * * *"]
